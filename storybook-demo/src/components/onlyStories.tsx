interface Props {}

function OnlyStories(props: Props) {
    const {} = props

    return (
        <div>This component uses Storybook stories exclusively with Chakra CSS</div>
    )
}

export default OnlyStories
